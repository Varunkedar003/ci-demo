pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building project...'
                sh 'ls'
                sh 'cat app.txt'
            }
        }
    }
}
